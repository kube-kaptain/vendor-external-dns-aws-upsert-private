# Source: external-dns/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vendor-external-dns-aws-upsert-private
  namespace: ${Environment}
  labels:
    app.kubernetes.io/name: vendor-external-dns-aws-upsert-private
    app.kubernetes.io/instance: vendor-external-dns-aws-upsert-private
    app.kubernetes.io/version: "${Version}"
    app.kubernetes.io/managed-by: Kaptain
    app: vendor-external-dns-aws-upsert-private
    app.kubernetes.io/part-of: ${ProductName}
    kaptain.org/environment: ${Environment}
    kaptain.org/product: ${ProductName}
    kaptain.org/project-name: ${ProjectName}
    kaptain.org/version: "${Version}"
    kaptain.org/owner: kube-kaptain
  annotations:
    kaptain.org/helm-upstream-chart: external-dns-1.21.1
    kaptain.org/generated-by: Generated by Kaptain vendor-helm-render-and-process
    kaptain.org/source-repository: kube-kaptain/vendor-external-dns-aws-upsert-private
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ${ProjectName}
      app.kubernetes.io/instance: vendor-external-dns-aws-upsert-private
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ${ProjectName}
        app.kubernetes.io/instance: vendor-external-dns-aws-upsert-private
    spec:
      automountServiceAccountToken: true
      serviceAccountName: vendor-external-dns-aws-upsert-private
      securityContext:
        fsGroup: 65534
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: external-dns
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            privileged: false
            readOnlyRootFilesystem: true
            runAsGroup: 65532
            runAsNonRoot: true
            runAsUser: 65532
          image: ${VendorExternalDnsAwsUpsertPrivate/EnvironmentDockerRegistryAndNamespace}/${DockerImageName}:${DockerTagExternalDns}
          imagePullPolicy: IfNotPresent
          args:
            - --log-level=${VendorExternalDnsAwsUpsertPrivate/LogLevel}
            - --log-format=${VendorExternalDnsAwsUpsertPrivate/LogFormat}
            - --interval=${VendorExternalDnsAwsUpsertPrivate/Interval}
            - --source=service
            - --source=ingress
            - --source=crd
            - --policy=upsert-only
            - --registry=txt
            - --txt-owner-id=${ProjectName}
            - --managed-record-types=A
            - --managed-record-types=AAAA
            - --managed-record-types=CNAME
            - --managed-record-types=MX
            - --managed-record-types=NS
            - --managed-record-types=PTR
            - --managed-record-types=SRV
            - --managed-record-types=TXT
            - --provider=aws
            - --aws-zone-type=private
            - --txt-prefix=_${ProjectName}.
            - ${VendorExternalDnsAwsUpsertPrivate/EventsFlag}
            - --min-event-sync-interval=${VendorExternalDnsAwsUpsertPrivate/MinEventSyncInterval}
            - --aws-zones-cache-duration=${VendorExternalDnsAwsUpsertPrivate/AwsZonesCacheDuration}
            - --aws-evaluate-target-health
            - --min-ttl=${VendorExternalDnsAwsUpsertPrivate/MinTtl}
            - --namespace=${VendorExternalDnsAwsUpsertPrivate/Namespace}
          ports:
            - name: http
              protocol: TCP
              containerPort: 7979
          livenessProbe:
            failureThreshold: ${VendorExternalDnsAwsUpsertPrivate/ProbeLivenessFailureThreshold}
            httpGet:
              path: /healthz
              port: http
            periodSeconds: ${VendorExternalDnsAwsUpsertPrivate/ProbeLivenessPeriod}
            timeoutSeconds: ${VendorExternalDnsAwsUpsertPrivate/ProbeTimeoutSeconds}
          readinessProbe:
            failureThreshold: ${VendorExternalDnsAwsUpsertPrivate/ProbeReadinessFailureThreshold}
            httpGet:
              path: /healthz
              port: http
            periodSeconds: ${VendorExternalDnsAwsUpsertPrivate/ProbeReadinessPeriod}
            successThreshold: ${VendorExternalDnsAwsUpsertPrivate/ProbeReadinessSuccessThreshold}
            timeoutSeconds: ${VendorExternalDnsAwsUpsertPrivate/ProbeTimeoutSeconds}
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: ${VendorExternalDnsAwsUpsertPrivate/ProbeStartupPeriod}
            timeoutSeconds: ${VendorExternalDnsAwsUpsertPrivate/ProbeTimeoutSeconds}
            failureThreshold: ${VendorExternalDnsAwsUpsertPrivate/ProbeStartupFailureThreshold}
          resources:
            requests:
              cpu: "${VendorExternalDnsAwsUpsertPrivate/CpuRequest}"
              memory: "${VendorExternalDnsAwsUpsertPrivate/Memory}"
            limits:
              memory: "${VendorExternalDnsAwsUpsertPrivate/Memory}"
          env:
            - name: GOMEMLIMIT
              value: ${VendorExternalDnsAwsUpsertPrivate/GoMemLimit}
